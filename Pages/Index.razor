@page "/"
@inject NavigationManager Nav

@*
    The old Home (practice selector) page was dropped. "/" now just sends
    the app to its default screen: Manage → Matches.
*@

@code {
    protected override void OnInitialized() =>
        Nav.NavigateTo("/league/matches", replace: true);
}
